<% content_for :title, "New Todo" %>

<div class="row">
  <div class="col-md-6">
    <div class="card shadow-sm">
      <div class="card-header bg-primary text-white">
        <h3 class="card-title mb-0">
          <i class="fas fa-plus"></i> Create a New Todo
        </h3>
      </div>
      <div class="card-body">
        <%= render "form", todo: @todo %>
      </div>
    </div>
  </div>
</div>
