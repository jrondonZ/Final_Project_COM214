<% @todos.each do |todo| %>
  <div class="todo-item">
    <h3><%= todo.title %></h3>
    <p><%= todo.description %></p>
    <p>Status: <%= todo.completed ? "Completed" : "Pending" %></p>
  </div>

<% end %>